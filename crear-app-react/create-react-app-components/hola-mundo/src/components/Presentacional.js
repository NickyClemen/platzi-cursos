import React from 'react';

const Presentacional = () => <h1>Hola Mundo.</h1>
/* Lo que hace ésta función, es un return explícito: No se necesita encapsular entre paréntesis para poder 
retornar un valor. */

export default Presentacional;