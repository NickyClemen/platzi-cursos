/** Función: Encapsula una porción de código para que ejecute una tarea específica. Permite la reutilización.
Librerías (bibliotecas de código): Recopilan funciones.
return-type function-name(parameter declarations, if any) {
	declarations; // Inicializar variables.
	statements; // Instrucciones que se van a ejecutar.
}

1) return-type: Tipo de dato que va a retornar.
2) parameter: Valores que se le pasa a la función. Se puede poner solamente el tipo de dato (aunque es una mala práctica). */
int powerOfInteger(int base, int n) {}
