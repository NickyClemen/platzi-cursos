/** Permite saltar a distintos tipos de bloques de código. */
#include <stdio.h>
for(size_t i = 0; i < count; i++) {
	for(size_t i = 0; i < count; i++) {
		for(size_t i = 0; i < count; i++) {
			/* Ayuda con estructuras anidadas, en los que break no alcanza. */
			if(panic) {
				goto errorHandler;
			}
		}
	}
}

errorHandler;
