/* Para publicar en npm --> npm publish --access=public 
Si da un error, hay que hacer npm login.
Para chequear quién está logueado, hacer npm whoami */

/* En la carpera website, se instala por npm el proyecto que se subió al npm. 
Las referencias dentro del index.ts, hay que cambiarlas. */

/* npm i para instalar todos los módulos. */