/** La "h" de la extensión, corresponde a encabezado. */
// Directiva de preprocesador. Se utiliza para preguntar si ya está definido un símbolo. Se evita que se defina varias veces.
# ifndef __MY_LIB_H
# define __MY_LIB_H
void hello(const char *name); // Firma de la función: Nombre y parámetros que recibe.
# endif
