// Bibliotecas (libraries) standards.
#include <stdio.h> // Librería de inputs y outputs.
#include <conio.h> // Console input/output. Entradas y salidas en la consola.
#include <string.h> // Trabajar con strings.
#include <stdlib.h> /** system = Comunicar fuera de programa de C. atoi = Convertir strings/caracteres a un valor integer. atol = A long. alof = A float.
rand = Números enteros aleatorios. delay = Pausas en la ejecución. */
#include <math.h> // Operaciones matemáticas.
#include <time.h> // Fechas del sistema.
#include <ctype.h> // Manejo de caracteres.
#include <signal.h> // Manejar señales en el programa. Detectar cosas por fuera del programa (event).
#include <locale.h> // Ayuda a definir cuestiones locales del software. Pensado para configuraciones.
#include <errno.h> // Manejo de errrores.
#include <assert.h> // Imprimir dignósticos.
